import { useEffect, useState } from "react";

export default function MainPage() {
    const appName : String = "동국 ON/OFF";

    return (
        <div>
            <h1>{appName}</h1>
        </div>
    );
}